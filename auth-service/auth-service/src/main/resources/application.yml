server:
  port: 9004 # Chọn một cổng chưa được sử dụng

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://postgres-auth-db:5432/auth_db
    username: postgres
    password: ${POSTGRES_PASSWORD}
  jpa: # <-- Thụt ra một cấp
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
  instance:
    prefer-ip-address: true

# Khóa bí mật để ký JWT token, bạn có thể thay đổi thành một chuỗi phức tạp hơn
jwt:
  secret: ${JWT_SECRET}